name: aws-python
runtime:
  name: python
  options:
    virtualenv: venv
description: A quickstart project for pulumi infrastructure
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python
